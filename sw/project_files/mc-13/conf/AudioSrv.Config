/**
*  @file      AudioSrv.Config
*  @brief     Config data for audio server 
*  @project   iBT150 
*  @author    Johnny Fan
*  @date      15-Sep-2014
*  @copyright Tymphany Ltd.
*/

#include "AudioSrv.h"
#include "product.config"

/*****************************************************************
 * Config Data
 *****************************************************************/
/* the time (ms) per timeout signal */
#define AUDIO_SRV_TIMEOUT_IN_MS  10

/* measured in ms */
#define AUDIO_ACTIVE_DELAY_UNMUTE_TIME          500
#define AUDIO_ACTIVE_LINE_IN_JACK_SCAN_TIME     500
#define AUDIO_ACTIVE_MUSIC_DETECT_TIME          500
#define AUDIO_SWITCH_CHANNEL_DELAY_TIME         100

#ifdef AUDIO_MULTI_SOURCE
#ifdef BT_CHANNEL
    #define AUDIO_DEFAULT_CHANNEL  BT_CHANNEL
#else
    #define AUDIO_DEFAULT_CHANNEL  AUDIO_CHANNEL_2
#endif // BT_CHANNEL
#endif // AUDIO_MULTI_SOURCE

#ifdef HAS_BT_TONE
static const int16 AUDIO_BT_TONE_PROCESS_DELAY_MS[]=
{
    10,  //change channel
    10,  //send the tone cmd to BT, if there's noise before the tone, make this bigger
    50, //change vol
    0,  //not used, use the AUDIO_BT_TONE_TIME_MS arrar instead 
    20, //mute the vol
    50, //restore the channel if needed
    50, //restore the vol
};

//play the tone, if there's noise after tone, turn this delay time down
static const int16 AUDIO_BT_TONE_TIME_MS[] = 
{
    2500, //BT_TONE_RESET_PDL_CMD,//USED FOR LOW_BATTERY TONE
    3000, //BT_TONE_BAT_DOCK_CMD, //USED FOR POWER_ON TONE
    2000, //BT_TONE_AC_IN_CMD,    //USED FOR CONNECTED TONE
    5000, //BT_TONE_LIM_VOL_CMD,  //USED FOR PAIRING_START TONE
    0,    //BT_TONE_PAIRING_CMD,  //===========================
    4000, //BT_TONE_POWER_OFF_CMD,//USED FOR POWER_OFF TONE
    0,    //BT_TONE_CONNECTED_CMD,//===========================
    0,    //BT_TONE_PAIR_FAIL_CMD,//===========================
};
#endif //HAS_BT_TONE
                                               
